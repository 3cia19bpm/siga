                   <div class="seletor-minutos-opcoes" role="group" aria-label="Selecionar minutos">
                     <button type="button" data-minutos-credito="0" onclick="selecionarMinutosCredito(0)">00</button>
@@ -13604,16 +13956,21 @@
             </div>
           </div>
 
-          <div class="painel painel-extrato-banco-horas">
-            <div class="cartao-topo">
+          <div id="painelExtratoBancoHoras" class="painel painel-extrato-banco-horas">
+            <div class="cartao-topo topo-extrato-banco">
               <div>
                 <span class="rotulo-suave">CRÉDITOS E UTILIZAÇÕES</span>
                 <h3 style="margin:4px 0 0">Meu extrato</h3>
               </div>
-              <span class="legenda-seguranca-banco"><svg class="icone-svg" aria-hidden="true"><use href="#icone-banco-horas"></use></svg> sem duplicidade</span>
+              <div class="painel-cabecalho-acoes painel-acoes-banco-horas">
+                <span class="legenda-seguranca-banco"><svg class="icone-svg" aria-hidden="true"><use href="#icone-banco-horas"></use></svg> sem duplicidade</span>
+                <button id="alternadorExtratoBancoHoras" class="alternador-filtros-permutas" type="button" aria-expanded="true" aria-controls="conteudoExtratoBancoHoras" aria-label="Ocultar meu extrato" title="Ocultar meu extrato" onclick="alternarExtratoBancoHoras()"><svg class="icone-svg" aria-hidden="true"><use href="#icone-seta-direita"></use></svg></button>
+              </div>
             </div>
-            <div id="listaCreditosBancoHoras" class="lista-creditos-banco-horas">
-              <div class="vazio">Carregando banco de horas...</div>
+            <div id="conteudoExtratoBancoHoras">
+              <div id="listaCreditosBancoHoras" class="lista-creditos-banco-horas">
+                <div class="vazio">Carregando banco de horas...</div>
+              </div>
             </div>
           </div>
 
@@ -14772,7 +15129,7 @@
 
       function medir() {
         const visual = window.visualViewport;
-        /* R9.8.1: offsetTop do visualViewport oscila durante o rubber-band do WebKit.
+        /* R9.8.4: offsetTop do visualViewport oscila durante o rubber-band do WebKit.
            A altura visual deve ser medida sem transformar essa oscilação em layout. */
         const alturaVisual = visual ? Number(visual.height || 0) : 0;
         const altura = Math.ceil(Math.max(
@@ -14822,7 +15179,7 @@
       });
       if (window.visualViewport) {
         window.visualViewport.addEventListener('resize', atualizar);
-        /* R9.8.1: não recalcular layout durante o bounce/scroll do visualViewport. */
+        /* R9.8.4: não recalcular layout durante o bounce/scroll do visualViewport. */
       }
       document.addEventListener('DOMContentLoaded', () => {
         const app = document.getElementById('appShell');
@@ -14842,7 +15199,7 @@
 
 
     /* =======================================================
-       SIGA 5.9.0 — R9.8.1 • CONTROLADOR DE ELASTICIDADE
+       SIGA 5.9.0 — R9.8.4 • CONTROLADOR DE ELASTICIDADE
        Mantém a rolagem normal do documento e intercepta somente
        a tentativa de ultrapassar o topo/rodapé. Assim o WebKit
        não move a viewport nem o cabeçalho, mas o usuário preserva
@@ -17427,14 +17784,11 @@
           identidade.innerHTML = `<div><strong>${principalAtual}</strong><span>Crédito vinculado automaticamente ao seu cadastro</span></div><span class="status status-neutro">VOCÊ</span>`;
         }
       }
-      const formulario = $('formCreditoBancoHoras');
-      const toggle = $('toggleRegistroCreditoBancoHoras');
-      if (formulario && toggle && formulario.classList.contains('oculto')) {
-        formulario.classList.remove('oculto');
-        toggle.classList.remove('recolhido');
-        toggle.setAttribute('aria-expanded', 'true');
-        const textoBotao = toggle.querySelector('span');
-        if (textoBotao) textoBotao.textContent = 'ESCONDER';
+      if ($('painelCreditoBancoHoras') && $('alternadorRegistroCreditoBancoHoras')) {
+        alternarPainelRegistroCreditoBancoHoras(true);
+      }
+      if ($('painelExtratoBancoHoras') && $('alternadorExtratoBancoHoras')) {
+        alternarExtratoBancoHoras(true);
       }
     }
 
@@ -17599,16 +17953,30 @@
       }
     }
 
-    function alternarPainelRegistroCreditoBancoHoras() {
+    function alternarPainelRegistroCreditoBancoHoras(forcarExpandido) {
+      const painel = $('painelCreditoBancoHoras');
       const formulario = $('formCreditoBancoHoras');
-      const botao = $('toggleRegistroCreditoBancoHoras');
-      if (!formulario || !botao) return;
-      const recolher = !formulario.classList.contains('oculto');
-      formulario.classList.toggle('oculto', recolher);
-      botao.classList.toggle('recolhido', recolher);
+      const botao = $('alternadorRegistroCreditoBancoHoras');
+      if (!painel || !formulario || !botao) return;
+      const recolher = typeof forcarExpandido === 'boolean' ? !forcarExpandido : !painel.classList.contains('recolhido');
+      painel.classList.toggle('recolhido', recolher);
+      botao.setAttribute('aria-expanded', recolher ? 'false' : 'true');
+      botao.setAttribute('aria-label', recolher ? 'Mostrar registro de crédito' : 'Ocultar registro de crédito');
+      botao.setAttribute('title', recolher ? 'Mostrar registro de crédito' : 'Ocultar registro de crédito');
+      formulario.setAttribute('aria-hidden', recolher ? 'true' : 'false');
+    }
+
+    function alternarExtratoBancoHoras(forcarExpandido) {
+      const painel = $('painelExtratoBancoHoras');
+      const conteudo = $('conteudoExtratoBancoHoras');
+      const botao = $('alternadorExtratoBancoHoras');
+      if (!painel || !conteudo || !botao) return;
+      const recolher = typeof forcarExpandido === 'boolean' ? !forcarExpandido : !painel.classList.contains('recolhido');
+      painel.classList.toggle('recolhido', recolher);
       botao.setAttribute('aria-expanded', recolher ? 'false' : 'true');
-      const texto = botao.querySelector('span');
-      if (texto) texto.textContent = recolher ? 'MOSTRAR' : 'ESCONDER';
+      botao.setAttribute('aria-label', recolher ? 'Mostrar meu extrato' : 'Ocultar meu extrato');
+      botao.setAttribute('title', recolher ? 'Mostrar meu extrato' : 'Ocultar meu extrato');
+      conteudo.setAttribute('aria-hidden', recolher ? 'true' : 'false');
     }
 
     document.addEventListener('keydown', function(evento) {
@@ -21414,7 +21782,7 @@
       window.addEventListener('focus', atualizar);
       if (window.visualViewport) {
         window.visualViewport.addEventListener('resize', atualizar);
-        /* R9.8.1: evita re-medição do cabeçalho durante o bounce do iOS. */
+        /* R9.8.4: evita re-medição do cabeçalho durante o bounce do iOS. */
       }
       document.addEventListener('DOMContentLoaded', () => {
         const topo = document.querySelector('.topbar');
--- a/sw.js
+++ b/sw.js
@@ -1,4 +1,4 @@
-const CACHE_SIGA = 'siga-github-v5.9.0-r9.8.1';
+const CACHE_SIGA = 'siga-github-v5.9.0-r9.8.4';
 const ARQUIVOS_SIGA = [
   './',
   './index.html',
